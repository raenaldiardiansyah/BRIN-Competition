import type { AIScore as Data } from "../types";
export function AIScore({ score }: { score: Data }) { const max = score.max ?? 100; const percent = Math.max(0, Math.min(100, (score.value / max) * 100)); return <section aria-label={score.label} className="tw:rounded-xl tw:border tw:border-slate-200 tw:bg-white tw:p-4"><div className="tw:flex tw:items-baseline tw:justify-between tw:gap-3"><strong className="tw:text-sm tw:text-slate-800">{score.label}</strong><span className="tw:text-2xl tw:font-bold tw:text-slate-950">{score.value}<small className="tw:ml-1 tw:text-sm tw:text-slate-500">/ {max}</small></span></div><div className="tw:mt-3 tw:h-2 tw:overflow-hidden tw:rounded-full tw:bg-slate-100"><div className="tw:h-full tw:rounded-full tw:bg-blue-600" style={{ width: `${percent}%` }} /></div>{score.description ? <p className="tw:mt-2 tw:text-xs tw:text-slate-500">{score.description}</p> : null}</section>; }
