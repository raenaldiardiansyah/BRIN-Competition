import type { ReactNode } from "react";
export function AIAnalysisHeader({ eyebrow = "Analisis AI", title, description, children }: { eyebrow?: string; title: string; description?: string; children?: ReactNode }) { return <header className="tw:flex tw:items-start tw:justify-between tw:gap-6 tw:mb-8"><div><p className="tw:text-xs tw:font-bold tw:uppercase tw:tracking-widest tw:text-blue-600 tw:mb-2">{eyebrow}</p><h1 className="tw:text-3xl tw:font-bold tw:text-slate-950 tw:tracking-tight">{title}</h1>{description ? <p className="tw:mt-3 tw:max-w-2xl tw:text-slate-600 tw:leading-relaxed">{description}</p> : null}</div>{children}</header>; }
